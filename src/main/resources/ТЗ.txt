Новая задача: ROLE_ADMIN=TEACHER, ROLE_USER=PUPIL. Учитель создаёт
группу учеников, когда добавляеть ученик, ученик дольжен принят запрос.
Учитель выбирает вопросы через галочки, счетчик работает сколько вопросов
выбрал учитель. Потом эти вопросы как задача поставляет к группе учеников
или отдельно к ученику. Ученик видить эта задача, проходит тесты, результат
увидит сам ученик и учитель тоже. У Учителя есть таблица, который ученик
проходил тест, результат учеников, баллы, %, .... Учитель может повторно
задать эти тот самое тесты к этому ученик или ученики или к другой новый
ученик или группу учеников. Сохранить эту группу вопросов, что бы дать
этот тесты заново. Иметь возможности переименовать к группе вопросов.

Хорошая постановка — это уже мини-LMS (learning management system) с заданиями, группами и повторным использованием тестов. Ниже дам цельный архитектурный алгоритм: роли → сущности → жизненный цикл → backend → frontend → ключевые сценарии. Это не просто идеи — это схема, по которой можно сразу начинать реализацию.

🧠 Общая логика системы

Есть 3 уровня:

Учитель → формирует задание → назначает ученикам → ученики проходят → результаты сохраняются


Причём:

задания переиспользуемые

ученики могут принимать приглашения

есть группы

статистика хранится отдельно

✅ Роли
ROLE_TEACHER  (admin)
ROLE_PUPIL    (user)

🗂 Основные сущности БД
1️⃣ Group — группа учеников
Group
- id
- name
- teacher_id
- created_at

2️⃣ GroupMembership — связь ученик ↔ группа
GroupMembership
- id
- group_id
- pupil_id
- status (PENDING / ACCEPTED / REJECTED)


👉 учитель отправляет приглашение
👉 ученик принимает

3️⃣ AssignmentTemplate — шаблон задания

Это набор выбранных вопросов, который можно использовать повторно.

AssignmentTemplate
- id
- title
- teacher_id
- created_at

4️⃣ AssignmentTemplateQuestion
assignment_template_questions
- template_id
- question_id


👉 именно здесь хранится набор галочек учителя

5️⃣ Assignment — конкретное назначение
Assignment
- id
- template_id
- assigned_by_teacher
- assigned_at
- due_date (optional)

6️⃣ AssignmentRecipient — кому назначено
AssignmentRecipient
- id
- assignment_id
- pupil_id
- status (NEW / STARTED / FINISHED)

7️⃣ AssignmentAttempt — попытка ученика
AssignmentAttempt
- id
- assignment_id
- pupil_id
- started_at
- finished_at
- score
- percent

8️⃣ AssignmentAttemptAnswers
attempt_answers
- attempt_id
- question_id
- selected_answer_id
- is_correct

🔁 Жизненный цикл задания
🧑‍🏫 Учитель
1. Создание группы
Teacher → Create group
Teacher → Invite pupils


Статус:

PENDING → ACCEPTED

2. Создание шаблона задания

UI:

☑ вопросы
счётчик выбранных вопросов
название шаблона


Backend:

POST /teacher/templates

3. Назначение задания

Можно назначить:

→ одному ученику
→ группе
→ нескольким ученикам


Backend:

POST /teacher/assign


Создаёт:

Assignment
AssignmentRecipients

👨‍🎓 Ученик
Видит список задач:
GET /pupil/assignments


Статусы:

NEW
STARTED
FINISHED

Проходит тест
POST /assignment/start
POST /assignment/finish


Сохраняется попытка.

📊 Учитель видит аналитику
GET /teacher/assignment/{id}/results


Таблица:

Ученик	Баллы	%	Время
🧩 Backend API структура
Учитель
Группы
POST   /teacher/groups
POST   /teacher/groups/{id}/invite
GET    /teacher/groups

Шаблоны заданий
POST   /teacher/templates
PUT    /teacher/templates/{id}/rename
GET    /teacher/templates

Назначение
POST   /teacher/assign
GET    /teacher/assignments
GET    /teacher/assignment/{id}/results

Ученик
GET    /pupil/assignments
POST   /assignment/start
POST   /assignment/finish

🎨 Frontend сценарии
Учитель
Страница:
Ученики → группы → шаблоны → задания → статистика

Выбор вопросов
☑ checkbox список
выбранные: 12

Назначение
[выбрать группу]
[выбрать учеников]
назначить

Ученик
Мои задания
→ пройти
→ результат

🔁 Повторное назначение

Поскольку есть:

AssignmentTemplate


учитель просто:

выбирает шаблон → назначает снова

⚡ Почему такая архитектура правильная

✔ шаблоны переиспользуются
✔ попытки независимы
✔ масштабируемо
✔ аналитика чистая
✔ группы гибкие
✔ можно расширять

🧠 Расширения (будущее)

Можно добавить:

дедлайн

авто-оценку

рейтинги

сравнение учеников

групповые отчёты

Если хочешь — дальше могу дать:

✅ JPA entities
✅ SQL schema
✅ Spring controller/service слой
✅ фронтовые потоки
✅ ER-диаграмму
✅ sequence diagram
✅ алгоритм назначения
✅ алгоритм статистики

👉 полноценный UI тестирования
👉 таблицу результатов учителя
👉 аналитика
👉 live обновления
👉 drag-drop вопросов
👉 роли security UI
👉 modal workflow